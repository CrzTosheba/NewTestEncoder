#ifndef CO_HEATING_GRAPH_DISPLAY_H
#define CO_HEATING_GRAPH_DISPLAY_H

#include "lvgl.h"
#include "CO_heating_graph_menu.h"

#ifdef __cplusplus
extern "C" {
#endif

// Объявление изображений маркеров
LV_IMG_DECLARE(lv_im_point_white);           // Белый маркер
LV_IMG_DECLARE(lv_im_point_yellow_change_x); // Желтый маркер с иконкой изменения X
LV_IMG_DECLARE(lv_im_point_yellow_change_y); // Желтый маркер с иконкой изменения Y
LV_IMG_DECLARE(lv_im_point_yellow_x);        // Желтый маркер для оси X
LV_IMG_DECLARE(lv_im_point_yellow_y);        // Желтый маркер для оси Y

// Объявление шрифтов
LV_FONT_DECLARE(Roboto_bold_18);

// ========== Параметры графика (для левой части экрана) ==========

// Позиция и размер графика на экране
#define GRAPH_CHART_X_POS          40   // Координата X левого верхнего угла графика (отступ слева)
#define GRAPH_CHART_Y_POS          115  // Координата Y левого верхнего угла графика (отступ сверху)
#define GRAPH_CHART_WIDTH          400  // Ширина графика в пикселях
#define GRAPH_CHART_HEIGHT         265  // Высота графика в пикселях

// Пределы графика (диапазоны значений на осях)
#define GRAPH_CHART_MIN_X          -50  // Минимальное значение на оси X (Тнв °C)
#define GRAPH_CHART_MAX_X          30   // Максимальное значение на оси X (Тнв °C)
#define GRAPH_CHART_MIN_Y          0    // Минимальное значение на оси Y (Тпод °C)
#define GRAPH_CHART_MAX_Y          100  // Максимальное значение на оси Y (Тпод °C)

// Цвета графика
#define GRAPH_CHART_BG_COLOR       0x1E2528  // Цвет фона графика (темно-серый)
#define GRAPH_CHART_BORDER_COLOR   0xFFFFFF  // Цвет границы графика (белый) - используется для левой и нижней границ
#define GRAPH_CHART_RIGHT_BORDER_COLOR  0x565B5D  // Цвет правой границы графика (серый, как линии сетки)
#define GRAPH_CHART_LINE_COLOR     0x565B5D  // Цвет линий сетки графика (светло-серый)
#define GRAPH_CHART_DATA_COLOR     0xE2000F  // Цвет линии данных графика (красный)
#define GRAPH_CHART_DATA_WIDTH     3         // Толщина линии данных графика (в пикселях)

// Настройки границы графика
#define GRAPH_CHART_BORDER_SIDE    (LV_BORDER_SIDE_BOTTOM | LV_BORDER_SIDE_LEFT)  // Стороны графика, на которых рисуется белая граница (слева и снизу)
#define GRAPH_CHART_RIGHT_BORDER_WIDTH  8   // Толщина правой границы графика (в пикселях)
#define GRAPH_CHART_PADDING        0         // Внутренний отступ графика (0 = без отступа)

// Смещение маркеров точек относительно позиции точки на графике
// Используется для центрирования изображения маркера над точкой
#define GRAPH_MARKER_X_OFFSET      19   // Смещение маркера по оси X (влево) для центрирования над точкой
#define GRAPH_MARKER_Y_OFFSET      24   // Смещение маркера по оси Y (вверх) для центрирования над точкой

// Параметры пунктирных линий (горизонтальных и вертикальных)
#define GRAPH_DASH_LINE_WIDTH      5    // Длина каждого штриха в пунктирной линии (в пикселях)
#define GRAPH_DASH_GAP             10   // Расстояние между штрихами в пунктирной линии (в пикселях)
#define GRAPH_DASH_LINE_X_OFFSET   -1   // Горизонтальное смещение пунктирной линии (для вертикальной линии - не используется)
#define GRAPH_DASH_LINE_Y_OFFSET   2    // Вертикальное смещение пунктирной линии (опускает горизонтальную линию на 2px вниз для лучшего визуального выравнивания с центром точки)

// Цвета пунктирных линий
#define GRAPH_DASH_LINE_COLOR_NORMAL 0x808080  // Цвет пунктирной линии в обычном режиме (серый, почти белый) - когда точка выбрана, но не редактируется
#define GRAPH_DASH_LINE_COLOR_EDIT   0xFFFF00  // Цвет пунктирной линии в режиме редактирования (желтый) - когда точка редактируется

// Позиция заголовка графика ("ГРАФИК ОТОПЛЕНИЯ")
#define GRAPH_TITLE_OFFSET_X       200  // Смещение заголовка по оси X относительно левого края контейнера заголовка (для центрирования)
#define GRAPH_TITLE_OFFSET_Y       240  // Смещение заголовка по оси Y относительно верхнего края графика (для позиционирования под графиком)

// ========== Параметры подписей осей графика ==========

// Позиция подписи оси X (Тнв) - должна быть справа внизу графика
#define GRAPH_X_AXIS_LABEL_OFFSET_FROM_RIGHT  60  // Смещение подписи оси X от правого края графика (в пикселях, приблизительная ширина текста "Тнв °C" + небольшой отступ)
#define GRAPH_X_AXIS_LABEL_OFFSET_Y          -30   // Смещение подписи оси X по вертикали от нижнего края графика (в пикселях, положительное значение = вниз от нижнего края)

// Позиция подписи оси Y (Тпод) - должна быть слева вверху графика
#define GRAPH_Y_AXIS_LABEL_OFFSET_X           5 // Смещение подписи оси Y по горизонтали от левого края графика (отрицательное = влево от левого края, чтобы текст "Тпод" был виден полностью)
#define GRAPH_Y_AXIS_LABEL_OFFSET_Y          -10  // Смещение подписи оси Y по вертикали от верхнего края графика (в пикселях, положительное значение = вниз от верхнего края)

// Цвет и шрифт подписей осей
#define GRAPH_AXIS_LABEL_COLOR     0xFFFFFF  // Цвет текста подписей осей (белый)
// Шрифт подписей осей - используется Roboto_bold_18 (объявлен выше)

// ========== Параметры делений осей (ticks) ==========

// Настройки для отображения цифр на делениях осей
#define GRAPH_AXIS_TICK_COUNT_X    5    // Количество делений на оси X (включая начало и конец)
#define GRAPH_AXIS_TICK_COUNT_Y    6    // Количество делений на оси Y (включая начало и конец)
#define GRAPH_AXIS_TICK_COLOR      0xFFFFFF  // Цвет цифр на делениях осей (белый)
// Шрифт для цифр на делениях осей (используется тот же, что и для подписей осей - Roboto_bold_18)

// Позиции для цифр на делениях осей
#define GRAPH_X_TICK_LABEL_OFFSET_Y       5   // Смещение цифр оси X от нижнего края графика (в пикселях, положительное = вниз, отрицательное = вверх; чтобы цифры были выше, уменьшите это значение или сделайте отрицательным)
#define GRAPH_X_TICK_LABEL_CENTER_OFFSET  10  // Смещение для центрирования цифр оси X относительно деления (в пикселях, для выравнивания по центру)
#define GRAPH_Y_TICK_LABEL_OFFSET_X       -35 // Смещение цифр оси Y влево от левого края графика (отрицательное = влево, чтобы цифры были видны полностью)
#define GRAPH_Y_TICK_LABEL_CENTER_OFFSET  8   // Смещение для центрирования цифр оси Y относительно деления (в пикселях, для выравнивания по центру)

// ========== Функции для работы с графиком ==========

/**
 * @brief Инициализирует график отопления
 * Создает объекты графика, маркеров, пунктирных линий и заголовка.
 * Должна быть вызвана один раз при старте приложения.
 */
void co_heating_graph_display_init(void);

/**
 * @brief Очищает график отопления
 * Удаляет все объекты графика и освобождает ресурсы.
 * Должна быть вызвана при закрытии меню графика отопления.
 */
void co_heating_graph_display_cleanup(void);

/**
 * @brief Показывает график отопления на экране
 * Делает график видимым (если он был скрыт).
 */
void co_heating_graph_display_show(void);

/**
 * @brief Скрывает график отопления
 * Делает график невидимым, но не удаляет объекты.
 */
void co_heating_graph_display_hide(void);

/**
 * @brief Обновляет отображение графика в зависимости от позиции курсора и режима редактирования
 * 
 * @param cursor_index Индекс элемента меню, на котором находится курсор
 * @param is_editing Флаг, указывающий, находится ли пользователь в режиме редактирования
 * @param editing_param_index Индекс редактируемого параметра (-1 если не редактируется)
 * 
 * Эта функция обновляет:
 * - Цвет и иконку маркеров точек (белый в обычном режиме, желтый при редактировании)
 * - Видимость и цвет пунктирных линий (горизонтальных для Тнв, вертикальных для Тпод_CO)
 * - Позиции точек на графике при редактировании
 */
void co_heating_graph_display_update_cursor(int cursor_index, bool is_editing, int editing_param_index);

/**
 * @brief Обновляет позиции точек на графике
 * Пересчитывает и обновляет координаты всех точек на графике
 * в соответствии с текущими значениями параметров.
 * Используется при изменении значений параметров точек.
 */
void co_heating_graph_display_update_points(void);

#ifdef __cplusplus
}
#endif

#endif // CO_HEATING_GRAPH_DISPLAY_H

